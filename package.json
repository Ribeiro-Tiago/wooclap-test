{
  "name": "wooclap-test",
  "description": "Test for full stack developer position at wooclap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "app": "cd app && yarn start",
    "api": "cd api && yarn start",
    "db": "docker-compose up",
    "db:stop": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^5.2.0"
  },
  "engines": {
    "node": "12.18.0"
  }
}
